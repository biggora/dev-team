Confirmed: `src/` contains only `.gitkeep` — `src/slugify.js` genuinely does not exist, so the test failure is due to the missing implementation, not a typo or setup mistake in the test. This is the correct expected-red reason.

## Summary

Working in Mode A (acceptance-first / red) for the slugify slice per `docs/prd.md` (FR-001, FR-002, AC-001..AC-004). No `docs/design.md` exists in this project, so `docs/prd.md` was the sole authoritative source.

Created:
- `C:\Users\biggora\AppData\Local\Temp\ar-S4\test\slugify.test.js` — 4 `node --test` cases, one per acceptance criterion, requiring `../src/slugify.js` (does not exist yet).
- `C:\Users\biggora\AppData\Local\Temp\ar-S4\docs\test-plan.md` — traceability matrix mapping FR-001/FR-002 → AC-001..AC-004 → test scenarios, plus a "Not Covered" section (Unicode/i18n per PRD out-of-scope, additional edge cases deferred to Mode B).

No files under `src/` were created or modified (out of scope, confirmed still `.gitkeep`-only).

Status: DONE

Files changed: `test/slugify.test.js` (created), `docs/test-plan.md` (created)
Summary: Mode A — derived 4 failing acceptance tests from PRD criteria AC-001..AC-004 for the slugify slice (FR-001 basic slugify + punctuation handling + type validation, FR-002 trim/lowercase). Ran `npm test` and confirmed all tests fail because `src/slugify.js` does not exist (`Cannot find module '../src/slugify.js'`, code `MODULE_NOT_FOUND`) — the right reason (missing implementation), not a typo or setup error. Verified `src/` contains only `.gitkeep`, confirming no accidental pre-existing implementation was missed.
Evidence:
```
$ npm test
> prd-stub@1.0.0 test
> node --test

Error: Cannot find module '../src/slugify.js'
Require stack:
- C:\Users\biggora\AppData\Local\Temp\ar-S4\test\slugify.test.js
    at Object.<anonymous> (C:\Users\biggora\AppData\Local\Temp\ar-S4\test\slugify.test.js:9:21)
    ... code: 'MODULE_NOT_FOUND'

✖ test\slugify.test.js (74.0328ms)
ℹ tests 1
ℹ pass 0
ℹ fail 1
EXIT_CODE=1
```
The require failure at load time blocks all 4 individual `test(...)` blocks from running (Node collapses this into one file-level failure) — same expected-red reason applies to all four ACs below.
Criteria:
- AC-001 ("Hello World" → "hello-world"): EXPECTED-RED — blocked by `Cannot find module '../src/slugify.js'` (Evidence above)
- AC-002 ("Rock & Roll, Baby!" → "rock-roll-baby"): EXPECTED-RED — same Evidence
- AC-003 (non-string input throws TypeError): EXPECTED-RED — same Evidence
- AC-004 ("  Mixed CASE  " → "mixed-case"): EXPECTED-RED — same Evidence
Concerns: none — this is a straightforward, minimal-scope Mode A pass. When `src/slugify.js` lands, re-run `npm test`; if the module-load error disappears but any individual assertion still fails, that is a genuine implementation gap.
