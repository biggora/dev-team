Both the existing test suite and a manual smoke check pass. Implementation is complete.

Status: DONE

Files changed: C:\Users\biggora\AppData\Local\Temp\ar-S1\src\format.js (created)

Summary: Created `src/format.js` exporting `formatCurrency(amount)`, following the exact conventions of `src/math.js` ('use strict', CommonJS `module.exports`, guard-clause validation throwing `TypeError` for non-number/NaN input). Formats via `$${amount.toFixed(2)}` to produce a USD string with two decimals (e.g. 1234.5 → "$1234.50").

Evidence:
- `npm test` (in C:\Users\biggora\AppData\Local\Temp\ar-S1) → exit 0, "tests 3 / pass 3 / fail 0" (pre-existing math.js tests unaffected; test/ is owned by tester so no new test file was added by me)
- Manual smoke check via `node -e` → exit 0: `formatCurrency(1234.5)` → `"$1234.50"`, `formatCurrency(0)` → `"$0.00"`, `formatCurrency(1234.567)` → `"$1234.57"`, `formatCurrency('5')` threw TypeError ("amount must be a number"), `formatCurrency(NaN)` threw TypeError

Criteria: N/A: no PRD (docs/prd.md does not exist in this project, confirmed by task context and directory listing)

Concerns: none — task is fully within scope (only src/format.js created); no test file was created or modified per scope boundaries (tester agent owns test/).
