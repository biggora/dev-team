Created src/greet.js following the exact conventions of src/math.js. I did not touch src/math.js or anything under test/ (tester's scope), per the boundaries given.

Status: DONE_WITH_CONCERNS

Files changed: src/greet.js (created)

Summary: Implemented greet(name) returning "Hello, <name>!", throwing TypeError for any non-string input (mirrors assertNumbers pattern in src/math.js, adapted to a single-string-arg assertString helper). No dependencies added.

Evidence:
- `npm test` → exit 1, output: add sums two numbers PASS, add throws TypeError on non-numbers PASS, sub subtracts two numbers FAIL (AssertionError: 8 !== 2 at test/math.test.js:15, caused by a pre-existing bug in src/math.js line 18: sub returns a + b instead of a - b). This failure is unrelated to my change.
- Manual smoke check → node -e "require('./src/greet.js').greet(...)": greet('World') → "Hello, World!"; greet(42) → threw TypeError; greet(null) → threw TypeError. All three checks passed.

Criteria: N/A — no docs/prd.md in this project.

Concerns: The overall npm test run is red because of a pre-existing bug in src/math.js (sub returns a + b), which is outside my scope to fix. My own module has no automated test coverage yet since test/ belongs to the tester agent — I verified it manually instead. Recommend the tester agent add test/greet.test.js and, separately, someone should look at the sub bug in math.js (flagged, not fixed, since it's out of scope).
