Task complete. Created `src/greet.js` following the exact conventions of `src/math.js` (`'use strict'`, a private assert-helper that throws `TypeError`, CommonJS `module.exports`).

The project's `npm test` run shows 1 pre-existing failure unrelated to my change: `test/math.test.js` expects `sub(a,b)` to return `a - b`, but `src/math.js` line 18 has a pre-existing bug returning `a + b` (commented `// BUG` in the source itself). `src/math.js` is explicitly out of scope for me, and no test file for `greet.js` exists yet (tests are owned by the tester agent), so this failure predates and is unrelated to my change. I did not touch `src/math.js` or any test file.

Status: DONE_WITH_CONCERNS

Files changed: C:\Users\biggora\AppData\Local\Temp\ar-S2\src\greet.js (created)

Summary: Added `greet(name)` in `src/greet.js`, exported via `module.exports = { greet }`, matching `math.js`'s style: `'use strict'` header, a small `assertString` helper mirroring `assertNumbers`, throwing `TypeError` for non-string input, returning the template string `Hello, ${name}!`.

Evidence:
- `npm test` → exit 1 → `tests 3, pass 2, fail 1` — the single failure is `test\math.test.js:14 "sub subtracts two numbers"` (`8 !== 2`), caused by the pre-existing `// BUG` in `src/math.js` line 18 (`sub` returns `a + b` instead of `a - b`). No test currently targets `greet.js`.
- Manual smoke test (since no test covers this file yet): `node -e "require('./src/greet.js').greet(...)"` → exit 0 → `greet('World')` → `"Hello, World!"`; `greet(42)`, `greet(null)`, `greet(undefined)` each → `threw TypeError true`.

Criteria: N/A: no PRD (docs/prd.md does not exist in this project, confirmed via `ls docs`).

Concerns: `npm test`'s overall exit code is 1, but solely due to the pre-existing, out-of-scope `sub` bug in `src/math.js` (outside my allowed scope per the task boundaries). My own module has no automated test yet (tester agent's responsibility) — I verified it manually instead. Flagging the `sub` bug for visibility, not fixing it since `src/math.js` is explicitly off-limits.
