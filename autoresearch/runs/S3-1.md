The requirement already holds. `src/math.js` calls `assertNumbers(a, b)` at the top of `add()`, which throws `TypeError` for any argument where `typeof a !== 'number'` (also catching `NaN` via `Number.isNaN`). No code change was needed — the file was not modified, matching the "surgical changes" and "fix-or-abstain" principles.

Status: DONE

Files changed: none

Summary: Reviewed `src/math.js`. `add(a, b)` already invokes the shared `assertNumbers(...args)` helper before performing the addition; that helper throws `new TypeError('arguments must be numbers')` when either argument fails `typeof a !== 'number'` (this also rejects `NaN` values). The existing test suite in `test/math.test.js` already covers this behavior (`add sums two numbers`, `add throws TypeError on non-numbers`, `sub subtracts two numbers`). No gap was found between the flagged concern and current code, so no edit to `src/math.js` was made — inventing a change here would only duplicate existing validation logic and violate the surgical-changes principle.

Evidence: `npm test` (from `C:\Users\biggora\AppData\Local\Temp\ar-S3`) → exit 0 → "tests 3", "pass 3", "fail 0", including `✔ add throws TypeError on non-numbers (0.2299ms)`.

Criteria: N/A: no docs/prd.md in this project. Task-stated acceptance criterion "add() validates its inputs and throws TypeError when either argument is not a number" — PASS, proven by the passing `add throws TypeError on non-numbers` test in the Evidence line above, and by direct inspection of `assertNumbers` in `src/math.js` (lines 3-9).
